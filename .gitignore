# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Environments & secrets
.venv/
.env

# OS
.DS_Store

# Working data artifacts: large intermediates (116 MB), regenerable via pipeline/.
data/*.csv
data/*.npy
!data/.gitkeep

# Deployment artifacts ARE committed. Streamlit Community Cloud only gets the repo,
# so the app cannot build its own index there. 47 MB total, produced by
# pipeline/prepare_deploy.py.
!data/cars.parquet
!data/embeddings_f16.npy

# Logs
*.log
.pytest_cache/

# Local tooling
.claude/
.agents/

# Authoring scripts and generated documents, kept local
build_guide_docx.py
build_spec_docx.py
PROJECT_GUIDE.docx
PROJECT_SPEC.docx
